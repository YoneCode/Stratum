{
  "name": "stratum",
  "version": "0.0.0",
  "private": true,
  "description": "Arc-native agent commerce stack — see arc-dapp.md",
  "engines": {
    "node": ">=20.18.0",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@11.1.3",
  "scripts": {
    "build:contracts": "forge build --root contracts",
    "test:contracts": "forge test --root contracts -vvv",
    "fmt:contracts": "forge fmt --root contracts",
    "clean:contracts": "forge clean --root contracts"
  }
}
